<h1>Editing Book</h1>
<%= form_for(@books) do |f| %>
   	<h4>Title</h4>
	<%= f.text_field :title %>
	 <h4>Body</h4>
	 <%= f.text_area :body %>
   	<br><%= f.submit 'Update Book' %></br>
<% end %>
<%= link_to 'Show', book_path(@books.id) %>
<%= link_to 'Back', books_path %>
